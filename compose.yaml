services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      MAX_UPLOAD_BYTES: "10485760"
    volumes:
      - eccentric-data:/app/data
      - eccentric-output:/app/output
    restart: unless-stopped

volumes:
  eccentric-data:
  eccentric-output:
